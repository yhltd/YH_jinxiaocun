/* packageP/page/shengchanxian/shengchanxian.wxss */
page {
  height: 100%;
}

view {
  font-size: 28rpx;
}

button {
  font-size: 30rpx;
}

.table {
  display: inline-flex;
  flex-direction: column;
}

/* 弹窗内表格修改 */
.sv_list {
  white-space: nowrap;
  margin-top: 0px;
  height: 86%;
  background-image: url('https://7968-yhltd-hsxl2-1259412419.tcb.qcloud.la/caiwu3.png?sign=6a8a025631c6c2b803098f8aa20bc8c8&t=1760000876');
  background-repeat: no-repeat;
  background-size: cover;
}

::-webkit-scrollbar-thumb {
  border-radius: 10px;
  background-color: rgba(155, 153, 153, 0.589);
}

.table_header {
  display: inline-flex;
  z-index: 6;
  height: 100rpx;
  position: sticky;
  top: 0;
}

.title {
  height: 100%;
  margin-right: 23rpx;
  display: inline-block;
  margin-left: 10px;
}

.title_text {
  display: flex;
  width: 100%;
  border-right: 1rpx solid #F0F0F0;
  border-bottom: 1rpx solid #F0F0F0;
  justify-content: center;
  align-items: center;
  overflow-x: auto;
  background: linear-gradient(135deg, #5a7592 0%, #3a4a6c 100%);
  color: white;
  font-weight: bold;
  font-size: 35rpx;
}

.list_row {
  display: inline-flex;
  flex-direction: row;
}

.table_main {
  display: inline-flex;
  height: 7%;
}

.table_main_bottom {
  height: 7%;
  display: flex;
  align-items: center;
  border-bottom: 1rpx solid #E6E6E6;
  font-size: 32rpx;
  padding-left: 20rpx;
  font-weight: bold;
}

.list_view {
  display: flex;
  flex-direction: column;
  width: 200rpx;
  height: 70rpx;
  justify-content: center;
  align-items: center;
  background-color: white;
  color: black;
  border-right: 1rpx solid #F0F0F0;
  border-bottom: 1rpx solid #F0F0F0;
  line-height: 70rpx;
}

.list_view_fixed {
  display: flex;
  flex-direction: column;
  width: 200rpx;
  height: 70rpx;
  justify-content: center;
  align-items: center;
  background-color: white;
  color: black;
  border-right: 1rpx solid #F0F0F0;
  border-bottom: 1rpx solid #F0F0F0;
  line-height: 70rpx;
  position: sticky;
  left: 0;
  z-index: 5;
}

/*
  input
*/
.input_view {
  position: fixed;
  width: 90%;
  height: auto;
  top: 25%;
  background-color: white;
  border-radius: 10rpx;
  box-shadow: 10rpx 10rpx 10rpx black;
  z-index: 20;
  left: 5%;
}

.input_text_form {
  border: 5rpx solid #E6E6E6;
  margin: auto;
  width: 80%;
  height: 64rpx;
  align-items: center;
  margin-top: 5%;
  border-radius: 19rpx;
  padding-left: 3%;
}

.input_text_form2 {
  border: 5rpx solid #E6E6E6;
  margin: auto;
  width: 80%;
  height: 64rpx;
  align-items: center;
  margin-top: 5%;
  border-radius: 19rpx;
  padding-left: 3%;
  margin-bottom: 10%;
}

.input_text_form_small {
  border: 5rpx solid #E6E6E6;
  margin: auto;
  width: 30%;
  height: 64rpx;
  align-items: center;
  margin-top: 5%;
  border-radius: 19rpx;
  padding-left: 3%;
  color: black;
}

.bt_view {
  margin: auto;
  border-top: 1rpx solid #3D3D3D;
  width: 90%;
  height: 38%;
  margin-top: 10%;
}

.bt_form {
  background: linear-gradient(135deg, #5a7592 0%, #3a4a6c 100%);
  width: 39%;
  margin-top: 4%;
  float: left;
  margin-left: 7%;
  color: white;
  margin-bottom: 38rpx;
  border: none;
  border-radius: 10rpx;
  font-weight: bold;
}

.bt_form_hover {
  background: linear-gradient(135deg, #637e9b 0%, #42527a 100%);
  width: 39%;
  margin-top: 4%;
  float: left;
  margin-left: 7%;
  color: white;
  margin-bottom: 38rpx;
  border: none;
  border-radius: 10rpx;
  font-weight: bold;
}

/*
  new_List
*/
.newList {
  position: fixed;
  height: 100%;
  width: 100%;
  top: 0;
  background-color: white;
  z-index: 20;
  box-shadow: 10rpx 10rpx 10rpx black;
}

.input {
  width: 92%;
  height: 90rpx;
  line-height: 86rpx;
  border-bottom: 2px solid #E6E6E6;
  margin: auto;
  border-radius: 15rpx;
  font-size: 33rpx;
  padding-top: 22rpx;
  padding-left: 5%;
}

.input_text {
  width: 30%;
  height: 50rpx;
  line-height: 65rpx;
  text-shadow: 5rpx 5rpx 5rpx #D6D6D6;
  color: #424242;
  margin-left: 50rpx;
}

/*
  bottom_view
*/
.bottom_view2 {
  display: flex;
  bottom: 2%;
}

.bt_bottom {
  width: 18%;
  background: linear-gradient(135deg, #5a7592 0%, #3a4a6c 100%);
  color: #ffffff;
  height: 75rpx;
  margin: auto;
  border: 1rpx solid rgba(255, 255, 255, 0.2);
  border-radius: 10rpx;
  font-weight: bold;
}

.bt_bottom_hover {
  width: 23%;
  background: linear-gradient(135deg, #637e9b 0%, #42527a 100%);
  color: #ffffff;
  height: 75rpx;
  margin: auto;
  margin-top: 11%;
  border: 1rpx solid rgba(255, 255, 255, 0.2);
  border-radius: 10rpx;
  font-weight: bold;
}

/*
  mask
*/
.mask {
  visibility: visible;
  opacity: 1;
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.6);
  z-index: 19;
  transition: all 0.3s ease-in-out;
}

.choiceList {
  position: fixed;
  height: 70%;
  width: 100%;
  top: 30%;
  background-color: white;
  z-index: 20;
}

/*
  select_view
*/
.sel_view_item {
  width: 100%;
  height: 10%;
  display: flex;
  align-items: center;
  border-bottom: 1rpx solid #E6E6E6;
}

.sel_view_top_input {
  width: 23%;
  height: 65%;
  border: 1rpx solid #E6E6E6;
  border-radius: 10rpx;
  margin-left: 26rpx;
  margin-right: 26rpx;
  padding-left: 19rpx;
}

.lables {
  font-size: 40rpx;
  margin-left: 10rpx;
  margin-top: 50rpx;
  font-weight: 600;
  color: #3a4a6c;
}

.lables1 {
  font-size: 40rpx;
  margin-left: 90rpx;
  margin-top: 10%;
  font-weight: 600;
  color: #3a4a6c;
}

.lables_small {
  font-size: 40rpx;
  margin-left: 90rpx;
  margin-top: 30rpx;
  font-weight: 600;
  color: #3a4a6c;
}

.bottom_view {
  align-items: center;
  display: flex;
  bottom: 2%;
  background: linear-gradient(135deg, #e8f4f8 0%, #d4e8f2 100%);
}

.flex-wrp {
  display: flex;
  overflow-x: scroll;
  height: 10%;
  background: linear-gradient(135deg, #e8f4f8 0%, #d4e8f2 100%);
}

/* 奇偶行颜色区分 */
.list_view:nth-child(even) {
  background: rgba(74, 101, 130, 0.1);
}

.list_view:nth-child(odd) {
  background: rgba(255, 255, 255, 0.95);
}

/* 点击效果 */
.bt_bottom:active,
.bt_form:active {
  transform: translateY(-2rpx);
  box-shadow: 0 6rpx 16rpx rgba(42, 58, 92, 0.3);
  background: linear-gradient(135deg, #637e9b 0%, #42527a 100%);
}

/* 表格单元格文字样式 */
.list_text {
  color: #333;
  font-size: 30rpx;
  font-weight: 700;
  text-align: center;
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100%;
  border-right: 1rpx solid rgba(90, 117, 146, 0.1);
  padding: 0 10rpx;
}

.list_text:last-child {
  border-right: none;
}

/* 无数据提示 */
.no-data {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 200rpx 0;
  color: #999;
  font-size: 32rpx;
  font-weight: bold;
}

/* 删除按钮特殊样式 */
.bt_form[style*="background-color: #ff4d4f"] {
  background: linear-gradient(135deg, #ff6b6b 0%, #ee5a52 100%);
}

.bt_form[style*="background-color: #ff4d4f"]:active {
  background: linear-gradient(135deg, #ff7b7b 0%, #ff6a6a 100%);
}

/* 权限禁用状态 */
button[disabled] {
  opacity: 0.5;
  background: #ccc !important;
  color: #666 !important;
  border: 1rpx solid #ccc !important;
}

/* 加载状态 */
.loading {
  text-align: center;
  padding: 100rpx;
  color: #5a7592;
  font-size: 32rpx;
  font-weight: bold;
}

/* 表格标题对齐 */
.title_text {
  background: linear-gradient(135deg, #5a7592 0%, #3a4a6c 100%) !important;
  color: #ffffff !important;
  font-size: 32rpx !important;
  font-weight: 700 !important;
  text-align: center !important;
  text-shadow: 0 1rpx 2rpx rgba(0, 0, 0, 0.3) !important;
  align-items: center !important;
  justify-content: center !important;
  height: 100% !important;
  border-right: 1rpx solid rgba(255, 255, 255, 0.1) !important;
}

/* 输入框聚焦效果 */
.input_text_form:focus {
  background: rgba(255, 255, 255, 0.95);
  border-color: #5a7592;
  box-shadow: inset 0 1rpx 3rpx rgba(90, 117, 146, 0.3);
  outline: none;
}

/* 操作按钮样式 */
.bt_bottom {
  min-width: 120rpx;
  padding: 0 20rpx;
  margin: 0 10rpx;
  font-size: 28rpx;
}

/* 容器样式 */
.container {
  min-height: 100vh;
  padding-bottom: 40rpx;
}

/* 确保表格标题栏正确显示 */
.table_header {
  display: inline-flex;
  z-index: 6;
  height: 100rpx;
  position: sticky;
  top: 0;
  width: 100%;
}

/* 标题样式 */
.title {
  height: 100%;
  margin-right: 23rpx;
  display: inline-block;
  margin-left: 10px;
}

.title_text {
  display: flex;
  width: 100%;
  border-right: 1rpx solid #F0F0F0;
  border-bottom: 1rpx solid #F0F0F0;
  justify-content: center;
  align-items: center;
  overflow-x: auto;
  background: linear-gradient(135deg, #5a7592 0%, #3a4a6c 100%);
  color: white;
  font-weight: bold;
  font-size: 35rpx;
}

/* 表格内容 */
.list_row {
  display: inline-flex;
  flex-direction: row;
}

.table_main {
  display: inline-flex;
  height: 7%;
}

.list_view {
  display: flex;
  flex-direction: column;
  width: 200rpx;
  height: 70rpx;
  justify-content: center;
  align-items: center;
  background-color: white;
  color: black;
  border-right: 1rpx solid #F0F0F0;
  border-bottom: 1rpx solid #F0F0F0;
  line-height: 70rpx;
}

.list_view_fixed {
  display: flex;
  flex-direction: column;
  width: 200rpx;
  height: 70rpx;
  justify-content: center;
  align-items: center;
  background-color: white;
  color: black;
  border-right: 1rpx solid #F0F0F0;
  border-bottom: 1rpx solid #F0F0F0;
  line-height: 70rpx;
  position: sticky;
  left: 0;
  z-index: 5;
}

/* 奇偶行颜色区分 */
.list_view:nth-child(even) {
  background-color: rgba(257, 246, 250, 1);
}

.list_view:nth-child(odd) {
  background-color: rgba(255, 255, 255, 0.95);
}

/* 表格单元格文字样式 */
.list_text {
  color: #333;
  font-size: 30rpx;
  font-weight: 700;
  text-align: center;
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100%;
  border-right: 1rpx solid rgba(90, 117, 146, 0.1);
  padding: 0 10rpx;
}

.list_text:last-child {
  border-right: none;
}

/* 底部按钮区域 */
.bottom_view {
  height: 10%;
  align-items: center;
  display: flex;
  bottom: 2%;
  background: linear-gradient(135deg, #e8f4f8 0%, #d4e8f2 100%);
  position: fixed;
  width: 100%;
  left: 0;
  z-index: 10;
}

.bottom_view2 {
  display: flex;
  align-items: center;
  justify-content: flex-start;
}

/* 按钮样式 */
.bt_bottom {
  width: 23%;
  background: linear-gradient(135deg, #5a7592 0%, #3a4a6c 100%);
  color: #ffffff;
  height: 75rpx;
  margin: auto 10rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.2);
  border-radius: 10rpx;
  font-weight: bold;
  font-size: 28rpx;
}

.bt_bottom_hover {
  background: linear-gradient(135deg, #637e9b 0%, #42527a 100%);
}

/* 权限禁用状态 */
button[disabled] {
  opacity: 0.5;
  background: #ccc !important;
  color: #666 !important;
  border: 1rpx solid #ccc !important;
}

/* 点击效果 */
.bt_bottom:active:not([disabled]) {
  transform: translateY(-2rpx);
  box-shadow: 0 6rpx 16rpx rgba(42, 58, 92, 0.3);
  background: linear-gradient(135deg, #637e9b 0%, #42527a 100%);
}
/* ================= 弹窗内按钮样式 ================= */

/* 弹窗内的按钮容器 - 不使用固定定位 */
.van-popup .bottom_view {
  position: relative !important;  /* 改为相对定位 */
  bottom: auto !important;        /* 取消底部固定 */
  left: auto !important;          /* 取消左侧固定 */
  width: 100% !important;         /* 宽度100% */
  height: 100rpx !important;      /* 固定高度 */
  margin-top: auto !important;    /* 自动上边距，推到最下面 */
  background: linear-gradient(135deg, #e8f4f8 0%, #d4e8f2 100%) !important;
  z-index: 1 !important;
}

/* 弹窗容器 */
.van-popup--bottom {
  height: 60% !important;
  display: flex !important;
  flex-direction: column !important;
}

/* 弹窗内容区域 */
.van-popup--bottom > view {
  height: 100% !important;
  display: flex !important;
  flex-direction: column !important;
}

/* 弹窗内的滚动区域 */
.van-popup--bottom scroll-view {
  flex: 1 !important;
  overflow-y: auto !important;
  padding: 40rpx !important;
}

/* 调整标签间距 */
.lables {
  font-size: 32rpx !important;
  margin-left: 0 !important;
  margin-top: 30rpx !important;
  margin-bottom: 10rpx !important;
  font-weight: 600;
  color: #3a4a6c;
}

.lables1 {
  font-size: 32rpx !important;
  margin-left: 0 !important;
  margin-top: 30rpx !important;
  margin-bottom: 10rpx !important;
  font-weight: 600;
  color: #3a4a6c;
}

/* 弹窗内的输入框 */
.van-popup .input_text_form {
  border: 5rpx solid #E6E6E6;
  width: 100% !important;
  height: 80rpx;
  border-radius: 19rpx;
  padding-left: 20rpx;
  font-size: 28rpx;
  margin-bottom: 20rpx;
}

/* 最后一个输入框加底部间距 */
.van-popup .di-bottom {
  margin-bottom: 40rpx !important;
}

/* 弹窗内的按钮 */
.van-popup .bt_bottom {
  width: 30% !important;
  margin: 0 10rpx !important;
  height: 70rpx !important;
  font-size: 28rpx !important;
}

/* 弹窗内按钮悬停效果 */
.van-popup .bt_bottom_hover {
  background: linear-gradient(135deg, #637e9b 0%, #42527a 100%) !important;
}